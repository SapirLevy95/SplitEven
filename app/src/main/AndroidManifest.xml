<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.split_even">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CalculateExpenses"></activity>
        <activity
            android:name=".approveNewItem"
            android:label="@string/title_activity_approve_new_item"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ShoppingCart" />
        <activity android:name=".Main_menu" />
        <activity android:name=".register_page" />
        <activity android:name=".login_page" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>